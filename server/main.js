import { Meteor } from 'meteor/meteor';
import _ from 'lodash';
import faker from 'faker';
import { Members } from '../imports/api/addlgas.js';

Meteor.startup(() => {
  // code to run on server at startup


});
